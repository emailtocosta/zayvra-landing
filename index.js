import React from 'react';
import ReactDOM from 'react-dom/client';
import ZayvraLandingPage from './ZayvraLandingPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ZayvraLandingPage />
  </React.StrictMode>
);
